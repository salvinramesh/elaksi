# Backend environment (MySQL)
PORT=4000
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="mysql://elaksi:STRONG_PASSWORD@localhost:3306/elaksi_ateliers"
# Use a strong secret; include this token in 'x-admin-token' header for admin actions from the frontend
ADMIN_TOKEN=AlphaCentuari@2262
# CORS allowed origin for the frontend dev server
CORS_ORIGIN=http://localhost:5173